# Server
server:
  port: 8080
  servlet:
    context-path: /api

# Internacionalização
spring:
  messages:
    basename: messages
    encoding: UTF-8
    fallback-to-system-locale: false

  # JPA / Hibernate
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.OracleDialect
    open-in-view: false

  # Datasource (Oracle acadêmico)
  datasource:
    url: jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL
    username: rm556331
    password: 151005
    driver-class-name: oracle.jdbc.OracleDriver

  # IA - Spring AI
  ai:
    openai:
      api-key: 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
      base-url: https://api.openai.com/v1
      chat:
        model: gpt-3.5-turbo

  # RabbitMQ (CloudAMQP)
  rabbitmq:
    host: fly.rmq.cloudamqp.com
    port: 5671
    username: kqszsjwo
    password: 17lV0zkgsziQxqdK4ELaoJjemIf05M56
    virtual-host: kqszsjwo
    ssl:
      enabled: true

  # Segurança (usuário padrão do Spring Security)
  security:
    user:
      name: admin
      password: admin123

  # Cache
  cache:
    type: simple

# JWT
jwt:
  secret: c2tpbGw0Z3JlZW4tc2VjcmV0LWtleS1wYXJhLWp3dC0yNTZiaXRz

# Swagger
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

# Paginação padrão
spring:
  data:
    web:
      pageable:
        default-page-size: 10
        max-page-size: 50

# Testes e Dev
spring:
  devtools:
    restart:
      enabled: true
  main:
    allow-bean-definition-overriding: true

# Logging / Encoding
spring:
  output:
    ansi:
      enabled: always
logging:
  charset:
    console: UTF-8